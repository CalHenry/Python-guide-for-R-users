# Terminal

Le terminal est un logiciel pour intéragir directement avec le shell, l'odinateur.  
Pas d'interface, juste des lignes de commandes.
Inhospitalier au début, il faut le voir comme un outil, très versatile qui permet d'effectuer des actions précises et simplement (après tout c'est toujours au même endroit et avec des commandes).
De nombreux outils sont accessible uniquement via une CLI (command line interface).

Surement pas convaincu, voici comment j'utilise personnellement le terminal pour tout mes projets:
- Tout part du termial: je navigue dans le dossier de mon projet et je lance mon éditeur de code depuis le terminal.
- J'utilise git depuis le terminal
- Ma gestion des fichiers et des dossiers est dans le terminal (copier, coller, couper, créer, supprimer)
- Explorer le contenu d'un script, d'un fichier de données
- Rechercher un bout de phrase dans une dossier sans savoir où le bout se trouve 
- ...

Le terminal est largement personnalisable, utiliser des meilleurs outils et avoir un look plus moderne et avec des couleurs. Tu peux créer tes propres commandes avec des scripts et des alias, et tu peux télécharger des nouveauw outils pour améliorer ou remplacer les commandes de bases.  
Par example: `bat`, est un remplacement pour `cat` avec du synhtax higlighing.


Le terminal est également l'interface principale quand on travaille dans le cloud.  
Selon le provider, on peut avoir acces à une plateforme un dashboard...
Mais si on se connecte à une  machine dans un datacenter, le seul moyen de la contrôler est le terminal
Donc savoir se débrouiller dans le terminal même qu'un peu est important

## Commades les plus importantes:

### Fichiers et dossiers
- ls
- cd
- touch
- mkdir


### Spécifique à python/ data sciences
- uv
- pixi
- python


### Développement
- git
- code (vscode)


## Outils qui améliore l'expérience avec le temrinal
- eza (better ls)
- zoxide (better cd)
- bat (better cat)
- yazi (file manager)
- lazygit (git tui)





